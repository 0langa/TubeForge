<Window x:Class="TubeForge.App.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TubeForge update available" Icon="Assets/TubeForge.ico"
        Width="540" Height="390" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Image Source="Assets/TubeForge.png" Width="64" Height="64"
                   AutomationProperties.Name="TubeForge" />
            <StackPanel Margin="18,2,0,0">
                <TextBlock Text="Update available" FontSize="24" FontWeight="Bold" />
                <TextBlock x:Name="VersionText" Margin="0,6,0,0"
                           Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Update TubeForge now?" FontSize="18" FontWeight="SemiBold" />
                <TextBlock Margin="0,10,0,0" TextWrapping="Wrap" LineHeight="20"
                           Text="TubeForge will securely download and verify the official installer, close this version, install the update, then launch the new version." />
                <TextBlock Margin="0,10,0,0" TextWrapping="Wrap" FontSize="11"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="Settings, queue, Library history, and downloaded media remain unchanged." />
                <ProgressBar Height="6" Margin="0,18,0,0" Maximum="1"
                             Value="{Binding UpdateDownloadFraction, Mode=OneWay}"
                             Visibility="{Binding IsDownloadingUpdate, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBlock Text="{Binding UpdateStatus}" Margin="0,12,0,0" TextWrapping="Wrap"
                           Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="LaterButton" Style="{StaticResource SecondaryButton}" Content="Later"
                    Click="LaterButton_OnClick" Padding="18,10" IsCancel="True"
                    IsEnabled="{Binding CanDismissUpdatePrompt}" />
            <Button Style="{StaticResource PrimaryButton}" Content="Update now" Margin="10,0,0,0"
                    Command="{Binding UpdateNowCommand}" Padding="18,10" IsDefault="True"
                    AutomationProperties.Name="Download, install, and restart TubeForge" />
        </StackPanel>
    </Grid>
</Window>
